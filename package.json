{
  "name": "@lacussoft/cpf-fmt",
  "version": "1.0.3",
  "description": "Basic function to validate CPF (Brazilian ID document).",
  "author": "Julio L. Muller",
  "license": "MIT",
  "keywords": [
    "lacussoft",
    "cpf",
    "fmt",
    "formatar",
    "format",
    "br"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lacussoft/cpf-fmt.git"
  },
  "bugs": {
    "url": "https://github.com/lacussoft/cpf-fmt/issues"
  },
  "homepage": "https://github.com/lacussoft/cpf-fmt#readme",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run build:dist && npm run build:node",
    "build:dist": "parcel build src/dist.js -o cpf-fmt.js --no-minify && parcel build src/dist.js -o cpf-fmt.min.js",
    "build:node": "parcel build src/core/index.js  -d src/ -o index.js --no-source-maps --no-minify --target node",
    "test": "jest"
  },
  "dependencies": {
    "@lacussoft/num-only": "^1.0.4",
    "html-escaper": "^2.0.2"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.34.0",
    "@typescript-eslint/parser": "^2.34.0",
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0",
    "jest": "^25.5.4",
    "parcel-bundler": "^1.12.4",
    "typescript": "^3.9.7"
  }
}
